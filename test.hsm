// a basic fib program as an example.
main:
    mov $A, 1; // Curr
    mov $B, 0; // Last
loop:
    ADD $A, $B, $C;
    MOV $B, $A;
    MOV $A, $C;

    MOV $X, $A; // Export to X

    JMP loop;
how:
    HLT; // This isn't should never be reachable.